// Web-only runtime polyfills, imported for side effects by App.web.tsx.
// Empty for now -- the browsers this app targets already provide every API
// the app currently relies on (fetch, FormData, crypto.randomUUID).
export {};
